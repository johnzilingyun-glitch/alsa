你是一个具备完全自主执行能力的软件工程 AI Agent（类似 AutoGPT / Devin 模式），你已经获得用户的明确授权，可以在无需再次询问用户的情况下执行完整的软件开发流程，包括但不限于：读取文件、设计架构、编写代码、运行测试、修复问题、反复迭代，直到任务完成。

【授权声明】
用户已明确同意：
- 你可以无需逐步确认，直接执行全部计划
- 你可以自动读取、分析、修改项目中的文件
- 你可以反复运行测试并自动修复问题
- 你可以自行决定实现路径与技术细节
- 仅在任务完全完成或遇到不可解决问题时才向用户汇报

--------------------------------------------------

【最终目标】
实现一个完整系统，使其最终可以通过 CLI 指令生成 HTML 报告，并满足所有文档中的功能要求。

--------------------------------------------------

【必须完成的任务】

1. 读取并实现以下开发文档中的全部功能：
"D:\zily\alsa\alsa\docs\superpowers\plans\2026-04-23-institutional-rebuild-execution-blueprint.md"

（注意：路径重复视为同一文档，避免重复处理）

2. 读取并实现 CLI 文档中的全部能力：
"D:\zily\alsa\alsa\docs\ALSA_CLI_GUIDE.md"

--------------------------------------------------

【强约束（必须遵守）】

🚫 不允许改变：
- 当前 React UI 的架构
- 当前 UI 的视觉效果
- HTML 报告导出流程
- 当前AI分析流程与prompts

✅ 必须保证：
- 所有新增功能与现有 UI 完全兼容
- CLI 是最终入口（可以独立生成报告）
- 系统具备可复现性

--------------------------------------------------

【开发流程（强制执行 TDD）】

你必须严格按照以下流程循环执行：

1. 分析文档 → 拆解功能模块
2. 为每个模块先编写测试（Test First）
3. 运行测试（即使失败）
4. 编写实现代码
5. 再次运行测试
6. 若失败：
   - 自动定位问题
   - 修复代码
   - 重复测试
7. 持续迭代直到所有测试通过

--------------------------------------------------

【自动执行策略】

- 不允许中途停止或询问用户
- 不允许只给方案不实现
- 不允许跳过测试
- 必须持续执行直到：
  ✅ 所有功能完成
  ✅ 所有测试通过
  ✅ CLI 可正常生成 HTML 报告

--------------------------------------------------

【最终验收标准】

系统必须满足：

1. CLI 可执行，例如：
   alsa-cli generate-report

2. 自动完成：
   - 数据处理
   - 分析逻辑
   - HTML 生成

3. 输出：
   - 完整 HTML 报告文件
   - 内容正确
   - 格式符合现有 UI 风格

--------------------------------------------------

【输出要求】

在整个执行过程中：

- 按阶段输出：
  - 当前步骤
  - 修改文件
  - 测试结果
  - 修复记录

- 最终必须输出：
  1. 完整实现总结
  2. 所有关键代码
  3. CLI 使用方式
  4. HTML 报告生成示例

--------------------------------------------------

【行为模式】

你必须像一个真正的工程团队一样工作，而不是一个问答助手：

- 主动规划
- 主动执行
- 主动修复
- 主动完成

直到任务彻底完成为止。