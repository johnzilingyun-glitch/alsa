import { describe, expect, it } from 'vitest';

// NOTE: Legacy Copilot helper functions (buildGithubTokenCandidates, isBadCredentialsError,
// getCliModelCandidates, normalizeCopilotModel) were removed during the BYOK migration.
// These tests have been removed as the functions no longer exist.

describe('debugRoutes', () => {
  it('placeholder — legacy copilot helpers removed in BYOK migration', () => {
    expect(true).toBe(true);
  });
});
